# 課堂使用指令
"""
```
pip install -U discord.py discord bot開發的必要套件
```
```
pip install -U python-dotenv拿取.inv的東西用的
```
"""

# 課堂小筆記
"""
gitingnore是讓被忽略的檔案不會被git追蹤的工具，這樣就不會將這些檔案提交到版本控制系統中了，用法為在分枝符號那裏找到.env然後按右鍵選擇add to gitignore，這樣就會在.gitignore檔案中加入.env這個檔案的名稱。
這樣就可以避免將一些敏感的資訊或者不必要的檔案提交到版本控制系統中了，這樣就可以保持版本控制系統的整潔了。
@bot.event是和筆記圖片一樣的原理，但是是包裝上discord的官方模板(讓他可以運行的必要程式)
*是指所有的的意思
"""
